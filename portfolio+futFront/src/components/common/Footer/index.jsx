const Footer = ({children}) =>{
    return (
        <footer>
            <div>
                {children}
                <small>
                Todos os direitos reservados.
                </small>
            </div>
        </footer>
    )
};
export default Footer;