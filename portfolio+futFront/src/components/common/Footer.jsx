const Footer = ({children}) =>{
    return (
        <footer>
            <div>
                {children}
                <small>
                
                </small>
            </div>
        </footer>
    )
};
export default Footer;